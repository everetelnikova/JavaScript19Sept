Transplantation_work_reduce:
1) Разделы select формируются в цикле for() в зависимости от условий.
2) Ф-ия work() для изменения в sel2  выполняется только при изменениях в sel1.Не дает выбирать нужную емкость.
3) Ф-ия для выполнения ф-ий при загрузке страницы window.onload = выполнится только 1 раз, и если оставить эти функ-ии в атрибутах, возникает ошибка.
4)Ф-ия arr.reduce()  применение не ясно, не смогла применить на объекте, только на массиве ключей или значений.


Transplantation_loop:
1) Замкнуть переменную на функцию, а функцию на переменную, т.е. ограничеть область видимости данных для функции.Не ясен ситаксис.


Transplantation_work_random:
1) Я взяла рандомные округленные до целого значения от 1 до 20 включительно.Обозначила переменными в объекте емкости, но уже на выводе в select у меня были не значение переменной, а сама переменная. Как задать условия чтобы формируемые рандомные числа не были одинаковы? 
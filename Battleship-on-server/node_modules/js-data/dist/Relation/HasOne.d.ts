import { Relation } from '../Relation';
export declare class HasOneRelation extends Relation {
    findExistingLinksFor(relatedMapper: any, record: any): any;
    isRequiresChildId(): boolean;
    static TYPE_NAME: string;
}

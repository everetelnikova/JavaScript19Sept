1) Если переправа идет правильно не формируется статус и возращается значение undefined.
2) при каждом нажатии кнопки происходит обновление данных в таблице, а не подстройка
3) что за событие, КАК использовать addEventListener()